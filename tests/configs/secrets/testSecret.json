{
    // OPTIONAL: You don't need this object if you don't want to check a database.
    "databaseConnectionOptions": {
        "dbms": "postgresql",
        "host": "YOUR DB HOST PATH",
        "port": 12345,
        "user": "YOUR DB USER NAME",
        "password": "YOUR DB PASSWORD",
        "database": "YOUR DB NAME"
        // OPTIONAL: If you need to use an SSL certificate. Path is relative to the secrets directory.
        //"sslCertificatePath": "./ca-certificate.cert"
    }
}